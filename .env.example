# Bot credentials
DISCORD_TOKEN=
GITHUB_TOKEN=
OPENAI_API_KEY=
ASSISTANT_ID=
M4M_DISCORD_API_KEY=
USER_ROLE_SYNC_ENABLED=true

# PostgreSQL connection (required). Use the docker-compose service name when
# running inside containers, or localhost when running Python directly.
DATABASE_URL=postgresql+psycopg://mantis:mantis@localhost:5432/mantis
POSTGRES_DB=mantis
POSTGRES_USER=mantis
POSTGRES_PASSWORD=mantis

# Optional; otherwise the bot finds a category/channel named "Teams"/"teams".
TEAMS_CATEGORY_ID=
TEAMS_DIRECTORY_CHANNEL_ID=

ORACLE_API_KEY=
